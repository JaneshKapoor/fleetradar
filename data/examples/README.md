# Example structured output

Real output from each pipeline stage, committed as the "example structured
output" the hackathon submission requires. Each file is genuine pipeline output,
trimmed for readability.

| File | Stage | What it shows |
|---|---|---|
| `changelog-raw.json` | 2 | Collector A raw extraction |
| `eol-raw.json` | 3 | Collector B raw extraction |
| `findings.json` | 5 | Classifier verdicts |
| `radar.json` | 6 | Final per-host radar entries |

Populated as each stage lands.
